name: deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: prd
    steps:
      - name: Trigger Portainer GitOps redeploy
        run: curl -X POST "${{ secrets.PORTAINER_WEBHOOK_URL }}"
